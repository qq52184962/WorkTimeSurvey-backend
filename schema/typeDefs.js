const Query = `
  type Query {
    placeholder: Boolean # For Schema Composition
  }
`;

const Mutation = `
  type Mutation {
    placeholder: Boolean # For Schema Composition
  }
`;

module.exports = [Query, Mutation];
